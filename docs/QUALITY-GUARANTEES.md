# 品質保証台帳

「完了済みの状態を継続的に再検証したい」不変条件の意図・台帳。機械検証できる保証は
テストスイート（実体が強制手段・CI が毎回再検証）で表現し、その意図をここに 1 行登録する。

各行は次の列を持つ:

- **保証したい状態**: 何が満たされ続けるべきか
- **source**: 契機（要件等の安定アンカー。Issue 番号は書かない）
- **強制手段**: 再検証を担うテスト等のポインタ
- **retire 条件**: この保証を台帳から外してよくなる条件

| 保証したい状態 | source | 強制手段 | retire 条件 |
|---|---|---|---|
| extra 未導入環境でも `import oai_agentspec.exceptions` が壊れず、`agents` / 各 extra 依存および `runtime.lightning` / `runtime.cli` を `sys.modules` に載せない（PEP 562 遅延の実効性） | 独自例外の統一窓口（`oai_agentspec.exceptions`） | `tests/test_extra_isolation.py` の subprocess 隔離テスト | 例外統一窓口の遅延取得方式を廃止し全例外を直 import に統一した場合 |
| `import oai_agentspec.runtime.resilience` 時点で実装実体 `_adapters.resilience` を `sys.modules` に載せない（PEP 562 遅延の実効性。`_adapters/__init__.py` がトップレベルで `.resilience` を import する退行も検知する） | ADR-0020 | `tests/runtime/resilience/test_init_pep562_l1.py::test_importing_window_does_not_load_adapter_module` / `::test_dir_call_does_not_load_adapter_module` | resilience 窓口の遅延取得方式（`_DEFERRED_SYMBOLS` の `__getattr__`）を廃止し全シンボルを直 import に統一した場合 |
| `_ChainedHooks` は SDK `RunHooksBase` の全 on_* メソッドをオーバーライドする（SDK 追随漏れによる silent gap 防止） | hooks 合成ヘルパー（`oai_agentspec.runtime.hooks.chain_hooks`） | `tests/runtime/hooks/test_chain_hooks_l2.py::test_chained_hooks_overrides_match_sdk_on_methods` | `chain_hooks` を廃止し利用者責務に戻した場合 |
| `_ChainedAgentHooks` は SDK `AgentHooksBase` の全 on_* メソッドをオーバーライドする（SDK 追随漏れによる silent gap 防止） | agent 単位 hooks 合成ヘルパー（`oai_agentspec.runtime.hooks.chain_agent_hooks`） | `tests/runtime/hooks/test_chain_agent_hooks_l2.py::test_chained_agent_hooks_overrides_match_sdk_on_methods` | `chain_agent_hooks` を廃止し利用者責務に戻した場合 |
| `chain_agent_hooks` は run 単位フック（`RunHooksBase` インスタンス・両基底継承を含む）を build 時に `TypeError` で拒否する（on_start / on_end の silent skip と on_handoff の from/to 反転による誤記録の防止） | agent 単位 hooks 合成ヘルパーの受理集合（ADR-0017） | `tests/runtime/hooks/test_chain_agent_hooks_l2.py::test_chain_agent_hooks_rejects_run_scope_hook` / `::test_chain_agent_hooks_rejects_hook_inheriting_both_scopes` / `tests/_adapters/test_governance_l2.py::test_govern_spec_rejects_run_scope_hooks_in_spec_hooks` | run 単位と agent 単位のメソッド名・引数構成が SDK 側で統一された場合 |
| `chain_agent_hooks` は `on_*` を 1 つも持たない要素を build 時に `TypeError` で拒否する（`*` の付け忘れ・型違いで全フックが無音で失われる silent gap の防止。1 つでも持つ部分実装は受理する） | agent 単位 hooks 合成ヘルパーの受理集合（ADR-0017） | `tests/runtime/hooks/test_chain_agent_hooks_l2.py::test_chain_agent_hooks_rejects_element_without_any_on_method` / `::test_chain_agent_hooks_rejects_unpack_omission` / `::test_chain_agent_hooks_accepts_element_with_single_on_method` | 部分実装の受理（duck-typed 委譲）自体を廃止した場合 |
| 上記 2 つの拒否判定に使うメソッド名集合が SDK `AgentHooksBase` の `on_*` と一致する（導出が過大になると誤って受理し、空になると `on_*` を持つ正当な要素まで全て拒否されるため、判定の土台そのものを pin する） | agent 単位 hooks 合成ヘルパーの受理集合（ADR-0017） | `tests/runtime/hooks/test_chain_agent_hooks_l2.py::test_agent_hook_method_names_match_sdk_lifecycle_methods` | 受理集合の build 時検証そのものを廃止した場合 |
| ガバナンス監査フックの合成順が `(監査, 既存フック)` である（fail-fast のため順序が反転すると既存フックの例外で監査記録が失われる。引数順は反転しても型が通るため挙動差で pin する） | AGT 監査フックの合成（ADR-0016） | `tests/_adapters/test_governance_l2.py::test_audit_record_precedes_inner_delegation` | 監査フックを合成でなく上書きで組む方式へ戻した場合 |
| `_ChainedAgentHooks` の fail-fast は SDK `AgentHooksBase` の 7 メソッドすべてで成立する（個別メソッドへ `try` / `except` を入れる退行を全メソッドで検知する） | agent 単位 hooks 合成ヘルパーの合成挙動（ADR-0016） | `tests/runtime/hooks/test_chain_agent_hooks_l2.py::test_chain_agent_hooks_stops_forwarding_when_element_raises`（`_METHOD_NAMES` で parametrize） | fail-fast を廃し例外集約方式へ変更した場合 |
| `prompt_slot(agent=None, layout=None, tune=<str>)` の旧 shape 呼び出しは `OptimizeError(CONFIG_MISSING)` で fail-closed される（新 shape 一本化・誤って旧経路が復活しないための regression guard） | prompt_slot の compose 一致化と旧経路削除（ADR 0007） | `tests/runtime/lightning/test_slots_l1.py::test_prompt_slot_rejects_legacy_shape` | ADR 0007 の either-or 契約（agent= か layout= 必須）が新 ADR で置き換えられた場合 |
| `examples/lightning/03_graph_apo.py` が import-safe で `build_registry` が構造的健全性を満たす | prompt_slot_factory 導入時の safety net 要件 | tests/examples/test_examples_smoke.py | 当該 example を publish から外した場合 |
| `optimize(target=HandoffGraph, slot=[...])` で graph routing 未到達 slot が pre-flight で `OptimizeError(CONFIG_MISSING, coverage=CoverageReport(...))` として fail-fast する（既定有効・`skip_coverage_check=True` で opt-out・silent no-op 防止） | graph routing 未到達 slot の silent no-op 防止（ADR 0009） | `tests/runtime/lightning/test_optimizer_l2.py::test_optimize_preflight_*`（未到達検知 / 全到達 pass / config・kwarg 両経路の opt-out と opt-out 時の pre-flight 未実行（rollout コスト回避）/ 生 seed 経路 skip / AgentSpec・WorkflowGraph target skip（allow-list: HandoffGraph のみ）/ interrupted 到達の union 算入・観測空 case の非寄与 / approvals・tool_mocks・context_factory の素通し / `logger.warning` 出力 / `per_case` 記録 / extra 不在は rollout 消費前に EXTRA_MISSING・観測中の実行時例外は pre-flight 標識付き TRAINER_FAILED / 観測中 ImportError は EXTRA_MISSING でなく TRAINER_FAILED（`::test_optimize_preflight_observation_import_error_maps_to_trainer_failed`）/ 観測失敗時の部分観測 warning に case 本文を含めない（`::test_optimize_preflight_partial_observation_logged_on_failure`）/ `timeout_seconds` 超過で TRAINER_FAILED（`::test_optimize_preflight_timeout_maps_to_trainer_failed`）/ `timeout_seconds=None` は上限を課さない（`::test_optimize_preflight_timeout_none_does_not_limit`）/ 観測失敗時の部分 `CoverageReport` が呼び出し側まで届く（`::test_optimize_preflight_partial_coverage_reaches_caller`）/ ループ外例外の安全網が `TRAINER_FAILED`・`coverage=None` へ倒す（`::test_optimize_preflight_outer_safety_net_maps_to_trainer_failed`））、`tests/runtime/lightning/test_rollout_l2.py::test_observe_route_steps_*`（fail-closed 2 経路: candidate 適用 None・`_CandidateInvalid` は `None` の無効化標識を返し到達を誤申告しない・実行済み観測空 `()` と区別する 3 値契約）、`tests/runtime/lightning/test_rollout_l2.py::test_check_route_coverage_*`（観測失敗時の部分 report 添付と `complete=False` / メッセージの case 位置・上限秒・例外型名 / 空本文で末尾コロンにしない / 観測中 `OptimizeError` を再ラップせず kind を保つ（承認 fail-closed の回帰 pin）/ case 本文の非露出 / 全件無効化で「未到達確定」を主張しない・部分無効化の件数分離・pass 時の invalid warning・観測完了数の非 None 計上（`::test_check_route_coverage_all_invalid_does_not_claim_unreached` 等））、e2e は `tests/runtime/lightning/test_optimizer_l2.py::test_optimize_preflight_all_invalid_reports_invalidation_not_unreached` | Phase 2 の `expected_route` 必須化で pre-flight rollout を retire した場合 |
| pre-flight coverage の診断型・設定が公開契約として維持される（`CoverageReport` の型と `per_case` の repr 抑止と 3 値（`None` = 無効化 / `()` = 実行済み観測空 / 非空 = 到達）・`complete` の既定 True と repr 露出・`invalid_cases` の既定 0 と repr 露出（`missing` の確定条件 = `complete and invalid_cases == 0`）・`OptimizeError.coverage` 属性・`OptimizeConfig.skip_coverage_check` の既定 False と kwarg 経路） | pre-flight coverage 診断型の公開契約（ADR 0009） | `tests/runtime/lightning/test_types_l1.py::test_coverage_report_*`・`::test_optimize_error_*coverage*`、`tests/runtime/lightning/test_config_l1.py::test_optimize_config_skip_coverage_check_*` | 当該公開シンボルを `runtime.lightning.__all__` から外した場合 |
| 複数 slot APO の途中失敗・スコア再計算失敗で、完了済み slot の最良テキストと履歴が `OptimizeError.partial`（`OptimizePartial`・`completed_slots` の repr 抑止・`failed_slot=None` = 全 slot 完了）として保全され、既送出 `OptimizeError` は kind / message を保ったまま partial 後付けされる（失敗時のデータ保全要件・ADR 0010） | 最適化失敗時の診断データ保全要件（ADR 0010） | `tests/_adapters/test_lightning_adapters_l2.py::test_run_apo_slot_failure_preserves_completed_slots` / `::test_run_apo_first_slot_failure_has_no_partial` / `::test_run_apo_slot_failure_optimize_error_passes_through_with_partial` / `::test_run_apo_score_failure_preserves_all_slots` / `::test_run_apo_partial_build_failure_falls_back_to_none`、型契約は `tests/runtime/lightning/test_types_l1.py::test_optimize_partial_*`・`::test_optimize_error_partial_*`、メッセージ整形は `::test_format_exception_message_*` と `tests/runtime/lightning/test_optimizer_l2.py::test_optimize_run_apo_boundary_message_carries_type_name` | agent-lightning が複数プロンプト同時最適化を提供し逐次実行を廃止した場合（ADR 0010 retire 条項） |
| `Exception` / `BaseException` / `ExceptionGroup` / `KeyboardInterrupt` / `SystemExit` / `asyncio.CancelledError` / `GeneratorExit` および `Exception` 非派生の `BaseException` 系を `FailsafePolicy.handlers` のキーにできない（build-time `ValueError`） | Failsafe の宣言的着地（ADR 0012） | `tests/runtime/resilience/test_failsafe_l1.py::test_failsafe_policy_handlers_Exceptionキーは_ValueError` / `::test_failsafe_policy_handlers_BaseExceptionキーは_ValueError` / `::test_failsafe_policy_handlers_KeyboardInterruptキーは_ValueError` / `::test_failsafe_policy_handlers_SystemExitキーは_ValueError` / `::test_failsafe_policy_handlers_CancelledErrorキーは_ValueError` / `::test_failsafe_policy_handlers_GeneratorExitキーは_ValueError` / `::test_failsafe_policy_handlers_Exception非派生のBaseException系キーは_ValueError` | 禁止列挙の方針自体を撤回する場合 |
| `failsafe_call` の thunk 受理契約違反（coroutine 直渡し・非 awaitable 戻り値）は `TypeError` で fail-fast し、`handlers` に `TypeError` を宣言していても着地しない | Failsafe の宣言的着地（ADR 0012） | `tests/runtime/resilience/test_failsafe_l1.py::test_failsafe_call_coroutine直渡しはTypeErrorでfail_fastする` / `::test_failsafe_call_coroutine直渡しのTypeErrorはTypeError宣言でも着地しない` / `::test_failsafe_call_非awaitableを返すthunkはTypeErrorでfail_fastする` / `::test_failsafe_call_非awaitable戻り値のTypeErrorはTypeError宣言でも着地しない` | thunk の受理契約を変更する場合 |
| `failsafe_call` は `handlers` 未宣言の例外を捕捉せずそのまま再送出する（例外チェーンの差し替えもしない） | Failsafe の宣言的着地（ADR 0012） | `tests/runtime/resilience/test_failsafe_l1.py::test_failsafe_call_未宣言例外はそのまま再送出される` / `::test_failsafe_call_handlers空は例外をそのまま伝播する` | 素通し方針を変更する場合 |
| `FailsafeResult.last_agent` の決定は 2 段（例外ごとの指定 `FailsafeHandler.last_agent` -> 全体規定 `FailsafePolicy.fallback_last_agent`）で、例外からの解決は `RUNNING_AGENT` を置いた段のみの opt-in（無指定ならどちらの段でも解決は走らず `None`） | Failsafe の last_agent 決定モデル（ADR 0013） | `tests/runtime/resilience/test_failsafe_l1.py::test_失敗時_段1も段2も未指定ならNone` / `::test_失敗時_段1未指定なら段2の具体agentが使われる` / `::test_失敗時_段1のRUNNING_AGENTが解決できれば段2より優先される` / `::test_失敗時_段1のRUNNING_AGENTが解決不能なら段2の具体agentへ落ちる` / `::test_failsafe_call_指定が無ければrun_dataを持つ例外でもlast_agentはNone` / `::test_failsafe_call_指定が無ければlast_agent属性を持つ例外でもNone` | 決定モデル自体（段数・opt-in 方式）を変更する場合 |
| `RUNNING_AGENT` は複製（`copy` / `deepcopy` / pickle 往復）でも同一インスタンスを保ち、`failsafe_call` / `FailsafeResult.from_exception` が生成する着地結果には sentinel そのものは載らない（解決済みの値か `None` のみ）。例外側の読み取り先（`exc.run_data.last_agent` / `exc.last_agent`）が sentinel を運んでいた場合も解決不能として次の読み取り先・次の段へ落ちる | Failsafe の last_agent 決定モデル（ADR 0013） | `tests/runtime/resilience/test_failsafe_l1.py::test_running_agent_はcopyで同一オブジェクトのままである` / `::test_running_agent_はdeepcopyで同一オブジェクトのままである` / `::test_running_agent_はpickle往復で同一オブジェクトのままである` / `::test_failsafe_call_deepcopyしたRUNNING_AGENTを段1に置いても解決値になる` / `::test_failsafe_call_解決値はRUNNING_AGENTそのものにはならない` / `::test_failsafe_call_例外属性のRUNNING_AGENTは解決値にならず段2へ落ちる` / `::test_failsafe_call_例外属性のRUNNING_AGENTで段2未指定ならNone` / `::test_failsafe_call_run_dataのRUNNING_AGENTは次の読み取り先へ進む` / `::test_failsafe_call_run_dataのRUNNING_AGENTで例外属性が無ければ段2へ落ちる` / `::test_failsafe_call_双方の読み取り先がRUNNING_AGENTならNone` / `::test_failsafe_call_sentinelを飛ばした先のfalsyな解決値は採用される` / `::test_from_exception_例外属性のRUNNING_AGENTは解決値にならずNoneになる` / `::test_from_exception_run_dataのRUNNING_AGENTは次の読み取り先へ進む` | sentinel の実装方式（`_RunningAgentSentinel`）を別方式に置き換える場合 |
| `RUNNING_AGENT` の着地値位置への誤配置（`FailsafeHandler.fallback` / `FailsafePolicy.handlers` の値位置）は build-time `ValueError`（反復と添字アクセスで内容が異なる独自 `Mapping` による検証迂回を含めて防止） | Failsafe の last_agent 決定モデル（ADR 0013） | `tests/runtime/resilience/test_failsafe_l1.py::test_failsafe_handler_fallbackへのRUNNING_AGENT宣言は_ValueError` / `::test_failsafe_policy_handlers値位置の直接RUNNING_AGENTは_ValueError` / `::test_failsafe_policy_値位置のdeepcopy済みRUNNING_AGENTも_ValueError` / `::test_failsafe_policy_反復と添字が食い違うMappingでも検証は迂回できない` | 誤配置ガードの方針を撤回する場合 |
| `_RunningAgentSentinel` は `__new__` により単一インスタンスのみを持ち、直接構築しても `RUNNING_AGENT` と同一オブジェクトになる（`is RUNNING_AGENT` を前提とした誤配置ガードを迂回できない） | Failsafe の last_agent 決定モデル（ADR 0013） | `tests/runtime/resilience/test_failsafe_l1.py::test_running_agent_直接構築してもモジュールsingletonを返す` / `::test_failsafe_handler_直接構築sentinelのfallback指定は_ValueError` / `::test_failsafe_policy_値位置の直接構築sentinelは_ValueError` | sentinel の実装方式を別方式に置き換える場合 |
| `failsafe_call` の thunk 受理契約検査は `handlers` の宣言有無より前で 1 度だけ行い、`handlers` が空でも lib のメッセージ（`thunk must return an awaitable`）で fail-fast する | Failsafe の宣言的着地（ADR 0012） | `tests/runtime/resilience/test_failsafe_l1.py::test_failsafe_call_handlers空でも非awaitable戻り値はlibのメッセージでfail_fastする` / `::test_failsafe_call_非awaitableのメッセージはhandlers空でも非空でも一致する` | thunk の受理契約を変更する場合 |
| `FailsafeResult` は直接構築でも `__post_init__` で `exception` / `matched_type` を検証し、`from_exception` と同一の契約・同一のメッセージになる | Failsafe の last_agent 決定モデル（ADR 0013） | `tests/runtime/resilience/test_failsafe_l1.py::test_failsafe_result_直接構築の非例外exceptionは_ValueError` / `::test_failsafe_result_直接構築の非例外クラスmatched_typeは_ValueError` / `::test_failsafe_result_直接構築の検証メッセージはfrom_exceptionと一致する` | 直接構築の検証方針を撤回する場合 |
| `last_agent` を保持する 3 フィールド（`FailsafeResult.last_agent` / `FailsafeHandler.last_agent` / `FailsafePolicy.fallback_last_agent`）は `repr` に出ない（機微の露出防止・`==` と `fields()` は不変） | Failsafe の last_agent 決定モデル（ADR 0013） | `tests/runtime/resilience/test_failsafe_l1.py::test_failsafe_result_reprにlast_agentが出ない` / `::test_failsafe_handler_reprにlast_agentが出ない` / `::test_failsafe_policy_reprにfallback_last_agentが出ない` | 機微マスキング方針を撤回する場合 |
| `failsafe_call` は `thunk` を経路（`handlers` 空 / 非空の正常完了 / 着地）によらず 1 回だけ呼び、その呼び出しは `try` の外で行う（`thunk` 本体が awaitable を返す前に同期送出した例外は宣言済みの型でも着地せず素通しし、監査も発火しない） | Failsafe の宣言的着地（ADR 0012） | `tests/runtime/resilience/test_failsafe_l1.py::test_failsafe_call_handlers空でもthunkの呼び出しは1回だけ` / `::test_failsafe_call_handlers非空の正常完了でもthunkの呼び出しは1回だけ` / `::test_failsafe_call_着地経路でもthunkの呼び出しは1回だけ` / `::test_failsafe_call_thunkが同期送出した宣言例外は着地せず伝播する` | thunk の呼び出し位置（try の外・1 回）の契約を変更する場合 |
| 着地時の監査 2 経路は独立して発火する（`log_on_apply=False` は warning のみを抑止し `on_apply` の呼び出しと `on_apply` 失敗時の error ログは止めない）。`on_apply` が送出した例外は sync / async（await 中）いずれも `logger.error(exc_info=True)` で握り潰し、`FailsafeResult` の返却を継続する | Failsafe の宣言的着地（ADR 0012） | `tests/runtime/resilience/test_failsafe_l1.py::test_failsafe_call_log_on_apply_Falseでwarningが出ない` / `::test_failsafe_call_log_on_apply_Falseでもon_applyは発火する` / `::test_failsafe_call_log_on_apply_Falseでもon_apply例外はerrorログに残る` / `::test_failsafe_call_on_apply例外はerrorログで握り潰され着地は継続する` / `::test_failsafe_call_async_on_applyの例外はerrorログで握り潰される` | 監査経路の失敗を呼び出し側へ伝播させる方針に変える場合 |
| fallback callable 自身が送出した例外は sync / async（await 中）いずれも素通しし、着地が成立していないため warning も `on_apply` も発火しない（`FailsafeHandler` 経由でも同一） | Failsafe の宣言的着地（ADR 0012） | `tests/runtime/resilience/test_failsafe_l1.py::test_failsafe_call_fallback自身の例外はそのまま伝播する` / `::test_failsafe_call_async_fallback自身の例外はそのまま伝播する` / `::test_failsafe_call_handler経由のfallback例外は素通しし監査も発火しない` / `::test_failsafe_call_handler経由のasync_fallback例外もそのまま伝播する` / `::test_failsafe_call_fallback例外時はwarningもon_applyも発火しない` | fallback 失敗時にも着地を成立させる方針に変える場合 |
| `last_agent` 解決が duck typing で読む SDK 側の構造（`AgentsException.run_data` の存在と None 既定 / `RunErrorDetails.last_agent` フィールド / `AgentsException` の `Exception` 派生）が SDK upgrade で silent に壊れない（読み取り先の消滅は duck typing では例外にならず「常に解決不能」へ静かに退行するため、実型を直接検査する） | Failsafe の last_agent 決定モデル（ADR 0013） | `tests/_adapters/test_resilience_exception_contract_l2.py` | 例外からの解決を廃止し `last_agent` を宣言のみで決める方式へ移行した場合 |
| 回答者 X のエントリから発動ルールを「一致 `source` -> 包括 -> なし」の順で選び、X への**最後の**ハンドオフ到達の遷移元で判定する（一致ルールが禁止のみなら包括へフォールバックしない） | Next-Turn Agent Override（ADR 0014） | `tests/test_next_turn.py::test_resolve_next_agent_一致sourceのルールが包括より優先される` / `::test_resolve_next_agent_不一致sourceでは包括ルールが選ばれる` / `::test_resolve_next_agent_一致ルールが禁止のみなら包括へ落ちない` / `::test_resolve_next_agent_最後の到達の遷移元で選定する` / `::test_resolve_next_agent_到達順を入れ替えると選定ルールも入れ替わる` / `::test_resolve_next_agent_X起点の循環でも最後の到達で選定する` / `::test_resolve_next_agent_X以外への到達は選定に影響しない` | 選定規則自体（優先順位・最後の到達で判定する方式）を変更する場合 |
| 到達時ハンドオフ禁止はハンドオフ到達でのみ発動し（直接開始では非発動・同一ターン内の再到達以降は発動）、到達記録は run 単位に分離されて次 run へ持ち越されない | Next-Turn Agent Override（ADR 0014） | `tests/_adapters/test_next_turn_registry_l2.py::test_禁止対象Xの出辺はis_enabledがcallableになる` / `::test_到達前はXの出辺が有効` / `::test_ハンドオフ到達後はXの出辺が無効化される` / `::test_到達記録は別のrunへ持ち越されない`、単体は `tests/_adapters/test_next_turn_l2.py::test_arrival_store_別のwrapperでは記録が分離される` / `::test_arrival_gate_別のrunでは委譲側に倒れる` / `::test_arrival_gate_記録済みならFalse` / `::test_arrival_gate_未記録なら既存Trueへ委譲する` | 実現形を `is_enabled` ゲート以外へ変更する場合 |
| 判定材料（`last_agent` / handoff アイテム）の属性欠落と属性アクセス自体の例外の双方を安全側（上書きなし / `None`）へ倒し、解決が例外を送出しない | Next-Turn Agent Override（ADR 0014） | `tests/_adapters/test_next_turn_l2.py::test_extract_turn_observation_last_agent属性の欠落はNoneへ倒す` / `::test_extract_turn_observation_last_agentのアクセス例外もNoneへ倒す` / `::test_extract_turn_observation_last_agentのstr変換例外もNoneへ倒す` / `::test_extract_turn_observation_new_items属性の欠落は空列へ倒す` / `::test_extract_turn_observation_new_itemsのアクセス例外も空列へ倒す` / `::test_extract_turn_observation_new_itemsが非iterableでも空列へ倒す` / `::test_extract_turn_observation_new_itemsのiter自体が例外でも空列へ倒す` / `::test_extract_turn_observation_遷移元のアクセス例外は当該アイテムのみスキップする` / `::test_extract_turn_observation_遷移先のアクセス例外は当該アイテムのみスキップする` / `::test_extract_turn_observation_判定材料が両方取れなくても例外にしない`、公開関数側は `tests/test_next_turn.py::test_resolve_next_agent_last_agent属性の欠落は上書きなし` / `::test_resolve_next_agent_last_agentのアクセス例外も上書きなし` / `::test_resolve_next_agent_new_itemsのアクセス例外も上書きなし` / `::test_next_turn_agent_last_agent属性の欠落はNoneを返す` / `::test_next_turn_agent_last_agentのアクセス例外でもNoneを返す` | 防御方針（安全側へ倒す）を変更する場合 |
| 依存する SDK 前提（run ごとの `RunContextWrapper` 新規生成・記録と参照の間のインスタンス同一性・handoff 有効性のステップ毎評価・無効 handoff のモデル非提示・`on_handoff` の署名検証・fork が別 wrapper・`RunResultStreaming` の判定材料同等性）が SDK upgrade で silent に壊れない | Next-Turn Agent Override（ADR 0014） | `tests/_adapters/test_next_turn_sdk_contract_l2.py`（15 件。記録と参照の同一性は `execute_handoffs` の実駆動で検証する）、`tests/_adapters/test_next_turn_streaming_contract_l2.py`（6 件） | 本機能が SDK のこれらの前提に依存しなくなった場合 |
| 動的エッジでは resolver が解決した候補が判定表に載るときだけ到達を記録し、候補を一括記録しない | Next-Turn Agent Override（ADR 0014） | `tests/_adapters/test_next_turn_registry_l2.py::test_動的エッジで判定表に載らない候補へ解決しても記録されない` / `::test_動的エッジ経由の到達でもXの出辺が無効化される` / `::test_動的エッジでも記録は利用者on_handoffより先に行われる` | 動的エッジの記録位置（`on_invoke` 内）を変更する場合 |
| 同一の誤宣言（`on_handoff` / `is_enabled`）は、禁止を宣言したエッジと判定表外エッジで同じ例外型・同じタイミングで落ちる（合成が非対称を持ち込まない） | Next-Turn Agent Override（ADR 0014） | `tests/_adapters/test_next_turn_registry_l2.py::test_誤宣言の例外型は禁止対象エッジと判定表外エッジで一致する`（`on_handoff` 側・build 時）/ `::test_誤宣言のis_enabledの例外型は禁止対象エッジと判定表外エッジで一致する`（`is_enabled` 側・run 時評価）/ `::test_到達記録済みなら誤宣言のis_enabledを評価せずFalseになる`（短絡が誤宣言に依存しないこと） | 合成の対称性原則（SDK と同じ規則・例外型・タイミングで落とす）を撤回する場合 |
| 判定表に載るエッジでも per-edge `HandoffConfig`（`description` / `tool_name` / `input_filter` / `options`）は合成後の `Handoff` に保持される（記録・ゲートの合成が他の宣言を落とさない） | Next-Turn Agent Override（ADR 0014） | `tests/_adapters/test_next_turn_registry_l2.py::test_記録を前置しても他のper_edge設定は保持される` / `::test_ゲート合成でも他のper_edge設定は保持される` | 合成方式（`dataclasses.replace` による差し替え）を変更する場合 |
| 判定表の設置プリミティブ `_install_next_turn_state` は freeze 後の呼び出しを拒否し、設置時に構築済み Agent キャッシュを破棄する | Next-Turn Agent Override（ADR 0014） | `tests/_adapters/test_next_turn_registry_l2.py::test_設置プリミティブは_frozen後はRegistryFrozenErrorになる` / `::test_設置プリミティブは構築済みAgentを破棄する` | 設置経路を freeze ガードなしの形へ変更する場合 |
| 包括ルールの到達元候補にファクトリ登録が含まれる警告は、当該ファクトリ源からの到達エッジに載る禁止対象を、もれなく・かつ余分なく名指しする（他の遷移元経由でのみ禁止が効く X は挙げない / 当該源が脅かす X は 2 件以上でもすべて挙げる） | Next-Turn Agent Override（ADR 0014） | `tests/test_next_turn.py::test_apply_next_turn_policy_警告はその到達元から効かない禁止対象だけを名指しする`（過大側）/ `::test_apply_next_turn_policy_警告はその到達元から効かない禁止対象をすべて名指しする`（過小側・切り詰め変異を kill） | 警告の名指し方式を変更する場合 |
| `GuardrailRegistry` の facade メソッド集合が同梱 guardrail factory 9 個と一致し、各 facade のシグネチャが対応 factory と許容差分 3 点（`name` の必須化・`labels` の追加・`severity` の追加）のみで同期している（写しのずれによる引数の取り違え・silent な非互換を防ぐ） | 宣言的ガードレール登録簿（ADR 0015） | `tests/runtime/guardrails/test_facade_sync_l2.py::test_facadeメソッド集合と同梱helper識別子集合は双方向一致する` / `::test_facadeとfactoryのシグネチャは許容差分3点を除いて完全一致する` | facade 経路を廃止し利用者が factory を直接呼ぶ形へ戻した場合 |
| 同梱 helper の既定分類データ `HELPER_DEFAULTS` のキー集合が「同梱 helper 識別子（公開窓口 `__all__` − 除外 3 集合）− 分類が DI 依存の helper」と一致している（helper 追加時の分類漏れを防ぐ） | 宣言的ガードレール登録簿（ADR 0015） | `tests/runtime/guardrails/test_facade_sync_l2.py::test_helper_defaultsのキー集合はfacade集合からdi依存分を除いたものと一致する` / `::test_di_dependent_helpersはfacade集合の部分集合である` | 分類データをコードから撤去し docs のみで保持する形へ戻した場合 |
| `docs/usage/safety/guardrails.md` の framework 分類表がコード側の分類データの投影として一致している（表の列構成が固定され、framework ラベル列が `HELPER_DEFAULTS` と一致し、「利用者が宣言」の行集合が `DI_DEPENDENT_HELPERS` と一致する。既定危険度列・適用境界列・備考列のセル値は照合対象外で、既定危険度は運用組織のポリシー判断で上書きされる出発点であることが除外の理由） | 宣言的ガードレール登録簿（ADR 0015） | `tests/runtime/guardrails/test_catalog_l1.py::test_docsのframework分類表は列構成を保つ` / `::test_docsのframeworkラベル列はhelper_defaultsと一致する` / `::test_docsの利用者宣言行はdi_dependent_helpersと一致する` | 分類表を docs から撤去し `HELPER_DEFAULTS` の import 案内のみへ変えた場合 |
| facade 経路で宣言される適用境界が、生成された実体から判定した境界と一致する（`register` 経路が課す不変条件を facade 経路にも課し、出力を検査するはずの宣言が入力側へ結線される silent な取り違えを防ぐ） | 宣言的ガードレール登録簿（ADR 0015） | `tests/runtime/guardrails/test_registry_l2.py::test_facadeの宣言境界は実体の境界と一致する`（facade 9 × `on` 2 値） | facade 経路を廃止し利用者が factory を直接呼ぶ形へ戻した場合 |
| `AgentSpec.guardrails` の名前参照は `AgentRegistry` 経由の build でのみ解決され、registry を経由しない build 経路（評価・最適化の単体 `AgentSpec` 対象）は無言で落とさず例外で拒否する（宣言した検査が存在しない対象を測ることを防ぐ） | 宣言的ガードレール登録簿（ADR 0015） | `tests/runtime/test_target_guardrail_refs_l2.py`（llmops / lightning を parametrize） | 名前参照の解決を registry 非依存の経路へ広げた場合 |
| 名前定数簿の宣言集合と `AgentRegistry` の登録名集合の差分が両方向・全件・単一 `KeyError` で報告される（片方向だけの報告は「1 箇所宣言」の前提を静かに崩し、タイポ防止が無効化しても検査が通る） | エージェント名定数簿の整合検査（ADR-0018） | `tests/test_agent_names_l1.py::test_整合検査は両方向の差分を単一_KeyError_で全件報告する` | 整合検査を廃止し `validate()` の実行時検出のみに戻した場合 |
| 決定的応答モデルは同一インスタンスの再実行・複数 Agent 共有で応答が変わらない（回数依存の状態が混入しても run は成功し続けるため挙動差でしか検知できない） | 決定的応答モデルのステートレス契約（ADR-0019） | `tests/_adapters/test_deterministic_model_l2.py::test_同一インスタンスの再実行と複数_Agent_共有で応答が変わらない` | 決定的応答モデルを廃止した場合、または状態を持つ設計へ変更しユーザー合意を得た場合 |
| 利用者向け公開面（コア `__all__` と `runtime/` 全窓口の `__all__`）に `Fake` / `Mock` / `Dummy` を含む名前が 0 件で、コア `__all__` に決定的応答モデル系シンボルが 0 件（識別子の混入はテストを壊さず静かに公開契約へ入る） | 決定的応答モデルの命名と公開窓口の割り当て（ADR-0019） | `tests/test_public_naming_l1.py::test_runtime配下の各公開窓口のall要素に禁止語彙が含まれない` / `::test_コアall__に禁止語彙が含まれない` / `::test_コアall__にテーマBの7シンボルが含まれない` | 公開窓口の割り当て方針を変更しコア `__all__` へ統合した場合 |
| 公開応答ビルダと公開ストリーミングイベントの既定 id 値 5 件（メッセージ id / ToolCall アイテム id / `call_id` / stream 終端の response id / 同 model 名）が `Fake` / `Mock` / `Dummy` / `workflow` / `wf` を含まない固定値である（id 値の変更は run を壊さず通るため pin が唯一の検知手段） | 応答ビルダとストリーミングイベントの既定 id 値（ADR-0019） | `tests/_adapters/test_deterministic_builders_l1.py::test_既定_id_値が禁止語を含まない固定値である` | 応答ビルダ・ストリーミングの id を利用者指定必須に変更し既定値を撤去した場合 |
| ターン境界は user / tool 側の発話で判定し、assistant 側 item 種別を列挙しない（allowlist にすると SDK が追加した種別（hosted tool 系等）が 1 応答を分断して 2 ターンと数えられ、ルール関数の `turn` 分岐が静かに外れる。run は成功し続けるため挙動差でしか検知できない） | 決定的応答モデルのターン境界判定（ADR-0019） | `tests/_adapters/test_deterministic_model_l2.py::test_hosted_tool_の_item_が挟まっても_1_応答は_1_ターンと数える` / `::test_user_text_は抽出できない入力では空文字列になる` / `::test_developer_ロールの_item_はモデル応答として数えない` | 決定的応答モデルを廃止した場合、または `turn` を公開契約から外した場合 |
| `ModelRequest.user_text` に user 由来でないデータ（tool 実行結果・system 文言）が載らない（載ると外部システムの戻り値が `if "..." in request.user_text` 形の遷移判定を成立させうる。値が変わっても run は成功するため pin が唯一の検知手段） | 決定的応答モデルの `user_text` の信頼境界（ADR-0019） | `tests/_adapters/test_deterministic_model_l2.py::test_user_text_に_tool_実行結果が混入しない` / `::test_user_text_は抽出できない入力では空文字列になる` | `user_text` を廃止し利用者が `input` から自前で抽出する設計へ変更した場合 |
| 宣言 dataclass の `bool` / `bool \| None` 注釈フィールド（`src/oai_agentspec/` 配下から `_adapters/` を除外・`init=True`）は、非 bool 値での構築が `ValueError` になる（truthiness 評価による「宣言したのに効かない」silent failure の防止。新しく追加されたフィールドが規則から静かに漏れる退行も検知する） | ADR-0021 | `tests/test_bool_fields_l1.py`（対象スコープの規則と同一走査で抽出したフィールドの parametrize 検証 + fixture map 網羅検査） | 宣言 dataclass の bool 型検証方針を撤回する新 ADR を受理した場合 |
| 入力に SDK item と見なせない要素（単体 dict を list 化したキー文字列列など）が混入しても `ModelRequest.turn` の「初回 0」契約が破れない。`Runner.run` と `get_response` 直接呼び出しの双方で成立する（例外も警告も出ずルール関数の初回分岐だけが静かに外れるため、pin が唯一の検知手段） | 決定的応答モデルの入力正規化とターン計上（ADR-0019） | `tests/_adapters/test_deterministic_model_l2.py::test_Runner_経由の_dict_単体入力でも導出フィールドが空値になる` / `tests/_adapters/test_deterministic_normalization_l1.py::test_input_items_は_dict_単体では戻り値の_list_検査で空列になる` | `turn` を公開契約から外した場合、または入力正規化を利用者責務へ移した場合 |
